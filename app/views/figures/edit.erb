<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Edit Figure</h1>
    <form class="" action="/figures<%= @figure.id %>" method="post">
      <strong>Name</strong>
      <input type="text" name="figure[name]" value="<%= @figure.name %>" id="figure_name">
      <br><br>

      <strong>Titles</strong>
      <br>
      <% Title.all.each do |title| %>
      <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" id="title_<%= title.id %>"<%='checked' if @figure.titles.include?(title) %>> <%= title.name %></input>
      <% end %>
      <br><br>

      <strong>Landmarks</strong>
      <br>
      <% Landmark.all.each do |lm| %>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%= lm.id %>" id="landmark_<%= lm.id %>"<%= 'checked' if @figure.landmarks.include?(lm) %>> <%= lm.name %></input>
      <% end %>
      <br><br>

      <strong>Create New Title</strong>
      <input type="text" name="title[name]" id="new_title">
      <br><br>

      <strong>Create New Landmark</strong>
      <input type="text" name="landmark[name]" id="new_landmark">
      <br><br>

      <input type="submit" id="Edit Figure">
      <br><br>
    </form>
  </body>
</html>